# GAME_LANDING_PAGE 🎮

Um projeto de **Landing Page de Jogo**, desenvolvido com **TypeScript**, **Vite**, e arquitetura moderna separando cliente, servidor e funcionalidades compartilhadas.

---

## Estrutura do Projeto 🗂️

- **client/** - Contém o código do front-end.
- **server/** - Contém o código do back-end.
- **shared/** - Código compartilhado entre front-end e back-end.
- **patches/** - Correções ou patches do projeto.
- Arquivos de configuração importantes:
  - `vite.config.ts` - Configuração do Vite.
  - `tsconfig.json` e `tsconfig.node.json` - Configuração do TypeScript.
  - `package.json` e `pnpm-lock.yaml` - Gerenciamento de pacotes.

---

## Instalação 💻

1. Clone o repositório:
   ```bash
   git clone https://github.com/devsparki/GAME_LANDING_PAGE.git
   cd GAME_LANDING_PAGE

pnpm install
pnpm dev

Scripts Disponíveis ⚡

pnpm dev - Inicia o servidor de desenvolvimento com hot-reload.
pnpm build - Compila o projeto para produção.
pnpm start - Inicia o projeto compilado em modo produção.

Tecnologias Utilizadas 🛠️

TypeScript
Vite
CSS moderno
Arquitetura cliente-servidor
